{
    "name": "Django Dev Container",
    "context": "..",
    "dockerFile": "Dockerfile",
    "postCreateCommand": "pip install -r requirements.txt && code --install-extension ms-python.python && code --install-extension ms-azuretools.vscode-docker",
    "runArgs": [
      "--cap-add=SYS_PTRACE",
      "--security-opt",
      "seccomp=unconfined"
    ],
    "mounts": [
      "source=${localWorkspaceFolder},target=/workspace,type=bind"
    ],
    "remoteUser": "vscode",
    "forwardPorts": [
      8000,
      5678
    ]
  }